# 🚀 Быстрый старт

## Для локальной разработки

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Создайте .env файл (скопируйте из .env.example)
cp .env.example .env
# Заполните все переменные в .env

# 3. Инициализируйте БД
python init_db.py

# 4. Запустите
python main.py
```

## Для деплоя на VPS сервер

**📖 Полная инструкция:** [DEPLOY.md](DEPLOY.md)

**Краткая версия:**

1. Загрузите файлы на сервер (git/scp/rsync)
2. На сервере:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   # Создайте .env файл с вашими ключами
   python init_db.py
   ```
3. Настройте systemd service (см. DEPLOY.md)
4. Запустите: `sudo systemctl start wb-reviews-agent`

**Сервер будет доступен по адресу:** `http://wb.1mlrd.ru:8002/`

---

## 📋 Что нужно перед запуском

- [ ] Ключ Wildberries API
- [ ] Ключ OpenRouter API  
- [ ] Токен Telegram бота
- [ ] Telegram Chat ID

**Как получить Chat ID:** Напишите боту, затем:
```
https://api.telegram.org/bot<TOKEN>/getUpdates
```
Найдите `"chat":{"id":123456789}` в ответе.

---

## ✅ Проверка работы

После запуска откройте:
- `http://localhost:8000/` - главная страница
- `http://localhost:8000/health` - health check
- `http://localhost:8000/stats` - статистика
- `http://localhost:8000/docs` - API документация

---

## 🔧 Полезные команды

```bash
# Просмотр логов (на сервере)
sudo journalctl -u wb-reviews-agent -f

# Перезапуск сервиса (на сервере)
sudo systemctl restart wb-reviews-agent

# Ручная обработка отзывов
curl -X POST http://localhost:8000/reviews/process
```

---

**Вопросы?** См. [DEPLOY.md](DEPLOY.md) для детальных инструкций.

