# WB Reviews Agent

Система автоматической обработки отзывов Wildberries с ИИ-генерацией ответов.

## Возможности

- ✅ Автоматическое получение отзывов из Wildberries API
- 🤖 Автоматическая генерация ответов через OpenRouter (для отзывов 4+ звезд)
- 📱 Telegram-уведомления для отзывов <4 звезд с возможностью подтверждения
- 🔁 Перегенерация ответов
- ✍️ Ручное редактирование ответов
- ⏰ Автоматическая проверка новых отзывов каждый час

## Установка

1. Клонируйте репозиторий или используйте текущую директорию

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните переменные окружения в `.env`:
   - `WB_API_KEY` - ключ API Wildberries
   - `OPENROUTER_API_KEY` - ключ OpenRouter
   - `TELEGRAM_BOT_TOKEN` - токен Telegram бота
   - `TELEGRAM_CHAT_ID` - ID чата для уведомлений

5. Инициализируйте базу данных:
```bash
python init_db.py
```

## Запуск

```bash
python main.py
```

Сервер запустится на `http://localhost:8000`

- API документация: `http://localhost:8000/docs`
- Health check: `http://localhost:8000/health`
- Статистика: `http://localhost:8000/stats`

## Структура проекта

```
wb-reviews-agent/
├── main.py                 # FastAPI приложение
├── config.py              # Конфигурация
├── init_db.py             # Скрипт инициализации БД
├── requirements.txt       # Зависимости
├── .env                   # Переменные окружения (создать)
├── database/
│   ├── models.py          # Модели БД
│   └── db.py              # Подключение к БД
├── services/
│   ├── wb_service.py      # Работа с WB API
│   ├── ai_service.py       # Работа с OpenRouter
│   └── telegram_service.py # Работа с Telegram
├── handlers/
│   └── review_handler.py  # Логика обработки отзывов
└── scheduler/
    └── tasks.py           # Планировщик задач
```

## API Endpoints

- `GET /` - Главная страница
- `GET /health` - Health check
- `GET /reviews` - Список отзывов
- `GET /reviews/{id}` - Детали отзыва
- `POST /reviews/process` - Ручная обработка отзывов
- `GET /stats` - Статистика

## Как это работает

1. **Планировщик** каждые N секунд (по умолчанию 3600 = 1 час) проверяет новые отзывы через WB API
2. Для каждого нового отзыва:
   - **Если рейтинг 4+ звезд**: ИИ генерирует ответ → автоматически публикуется
   - **Если рейтинг <4 звезд**: ИИ генерирует черновик → отправляется в Telegram с кнопками:
     - ✅ Опубликовать
     - 🔁 Перегенерировать
     - ✍️ Правка вручную
     - 🚫 Пропустить
     - 📎 Показать товар

## Получение Telegram Chat ID

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Напишите боту любое сообщение
4. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
5. Найдите `chat.id` в ответе - это ваш `TELEGRAM_CHAT_ID`

## Примечания

- Убедитесь, что структура ответа WB API соответствует ожидаемой (может потребоваться адаптация в `wb_service.py`)
- Настройте промпт для генерации ответов в `ai_service.py` под ваши нужды
- Для продакшена рекомендуется использовать PostgreSQL вместо SQLite

